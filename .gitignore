# ---- Python artefacts --------------------------------------------------
__pycache__/
*.py[cod]
*.so
.mcp.json
claude.md
.beads/*
.beads/issues.jsonl

# ---- Packaging ---------------------------------------------------------
*.egg-info/
build/
dist/

# ---- Environments & tooling -------------------------------------------
.env
.env.*
!.env.example
!.env.sample
.venv/
venv/
.idea/
.vscode/

# ---- macOS clutter -----------------------------------------------------
.DS_Store

# ---- Secrets -----------------------------------------------------------
client_secret.json
client_secrets.json
.mcpregistry_*_token

# ---- Logs --------------------------------------------------------------
mcp_server_debug.log

# ---- Local development files -------------------------------------------
/.credentials
/.claude
.serena/
Caddyfile
ecosystem.config.cjs
mcp-config.toml

# ---- Agent instructions (not for distribution) -------------------------
.github/instructions/

# SpecStory transcripts live in the private confidential repo (symlinked).
# Slash-less form is the ONLY one that matches a symlink; slash form covers
# a real directory on machines not yet migrated.
.specstory
.specstory/

# Agent scratch. Slash-less form matches a symlink; the slash form does not,
# so a directory-only rule silently stops matching if tmp becomes a link.
tmp
tmp/

# Linked worktrees (per agent-config convention: {project_dir}/worktrees/)
worktrees/
